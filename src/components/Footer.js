import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Footer = () => {
  return (
    <View>
      <Text>footer</Text>
    </View>
  );
};

export  { Footer };

const styles = StyleSheet.create({});
