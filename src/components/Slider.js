import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Slider = () => {
  return (
    <View>
      <Text>Slider</Text>
    </View>
  );
};

export  { Slider };

const styles = StyleSheet.create({});
