import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Login = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export { Login} ;

const styles = StyleSheet.create({});
